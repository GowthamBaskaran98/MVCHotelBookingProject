@model OnlineHotelBookingApplication.Models.UserViewModel

@{
    ViewBag.Title = "Portal";
    Layout = "~/Views/Shared/Master.cshtml";
}

<h2>Portal</h2>

<table border="1">
    <tr>
        <td>Firstname</td>
        <td>LastName</td>
        <td>MobileNumber</td>
        <td>Gmail</td>
        <td>Password</td>
        <td>UserType</td>
    </tr>
    @*@foreach (OnlineHotelBookingApplication.Models.UserViewModel user in (List<OnlineHotelBookingApplication.Models.UserViewModel>)ViewBag.Store)
    {
        <tr>
            <td>@user.FirstName</td>
            <td>@user.LastName</td>
            <td>@user.MobileNumber</td>
            <td>@user.Gmail</td>
            <td>@user.Password</td>
            <td>@user.UserType</td>
        </tr>
    }*@
</table>